"""Module for storing constants variables."""
import re

# <regexes>  ---------------------------------------------------

FILE_NAME_MATCH = re.compile(r"(?i).*\.(?:jpg|jpeg|png|gif|bmp|webp)$")

# </regexes> ---------------------------------------------------